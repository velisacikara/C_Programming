
#include <stdio.h>


/*

// DERS 33 - 34 (IF-ELSE)

int maxFinder(int a, int b);

void positiveFinder(int a);


int main()
{
	int n1, n2;

	printf("Enter two numbers: ");
	scanf_s("%d%d", &n1, &n2);

	printf("Bigger value is %d\n\n", maxFinder(n1, n2));
	
	positiveFinder(n1);
}

int maxFinder(int a, int b)
{
	if (a > b)
		return a;
	else
		return b;
}

void positiveFinder(int a)
{
	if (a < 0);		// Bu blokta herhangi bir iþlem yapýlmak istenmiyorsa sonuna ya da altýna ; koyulabilir.

	else
		printf("First value is positive");
}

*/

/****************/

/*
// DERS 35 (DANGLING ELSE)

int main()
{
	int n1, n2, n3, max;

	printf("Uc sayidan en buyugunu bulma\n\n");
	printf("Uc tane sayi giriniz: ");
	scanf_s("%d%d%d", &n1, &n2, &n3);

	if (n1 > n2)
		if (n1 > n3)
			max = n1;
		else
			max = n3;
	else
		if (n2 > n3)
			max = n2;
		else
			max = n3;



	printf("En buyuk sayi: %d", max);

}
*/

int main()
{

}
